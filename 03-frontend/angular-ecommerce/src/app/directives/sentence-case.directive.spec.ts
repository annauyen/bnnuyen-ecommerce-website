import { SentenceCaseDirective } from './sentence-case.directive';

describe('SentenceCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new SentenceCaseDirective();
    expect(directive).toBeTruthy();
  });
});
